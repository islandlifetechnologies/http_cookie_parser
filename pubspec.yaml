name: 'http_cookie_parser'
description: 'Parser that reads and splits the set-cookie comma joined value into actual separate cookies.'
homepage: 'https://github.com/islandlifetechnologies/http_cookie_parser'
version: 1.0.0+2

environment:
  sdk: ^3.12.0

dev_dependencies:
  ilt_lints: ^1.0.0+1
  test: ^1.31.2

